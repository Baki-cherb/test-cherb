Placez ici l'icône de l'application : icon.ico (256x256 recommandé).

Si le fichier icon.ico est absent, electron-builder utilisera l'icône
Electron par défaut — l'application se construit quand même.

Astuce : exportez un PNG 256x256 de votre logo puis convertissez-le en .ico.
